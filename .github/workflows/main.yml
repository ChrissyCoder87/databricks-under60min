name: Complete Infrastructure Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.7.0

      - name: Terraform Init
        run: terraform init infrastructure/environments/dev

      - name: Terraform Plan
        run: terraform plan infrastructure/environments/dev

      - name: Terraform Apply
        run: terraform apply -auto-approve infrastructure/environments/dev
